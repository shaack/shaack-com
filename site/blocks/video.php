
<section class="block block-video max-width-lg mx-auto container-fluid">
    <video preload="none" class="shadow" width="100%" height="auto" autoplay="autoplay" muted="muted" loop="loop">
        <source src="assets/videos/it-consulting.m4v" type="video/mp4"/>
        Your browser does not support the video tag.
    </video>
    <!--
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const video = document.querySelector("video")
            video.muted = true
            video.addEventListener("click", function () {
                if (video.paused) {
                    video.play()
                } else {
                    video.pause()
                }
            })
            video.play()
        })
    </script>
    -->
</section>